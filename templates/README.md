# Templates

This directory houses HTML templates and CSS styles (like `cv-template.html`) used by the PDF generation engine (`engine/html_compiler.py`). 

The pipeline uses these templates as the structural chassis for the tailored resumes, injecting the LLM-generated ATS-optimized content into them before rendering the final PDF.
