# Memory / Vector Database

This directory persists the local vector embeddings (e.g. ChromaDB data). 
It allows the `brain.py` (RAG engine) to perform fast semantic searches across the user's historical portfolio, prior resumes, and interview prep documents to augment the LLM context.
