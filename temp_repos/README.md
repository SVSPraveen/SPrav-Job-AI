# Temporary Repositories

This directory is used by the Intake and Analysis engines to temporarily clone user repositories (e.g., from GitHub). It allows local AST parsing, dependency analysis, and codebase summarization to extract technical achievements before the directory is cleaned up.
