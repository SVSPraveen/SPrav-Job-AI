# Configuration

This directory contains system-level configuration files or templates used across the SPrav application, such as LLM endpoint configs, database connection strings, or environment-specific defaults.
